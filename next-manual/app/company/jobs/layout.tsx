export default function jobs({
    children
}: {
    children: React.ReactNode
}) {
    return (
        <div>
            {children}
            ************jobs layout*************
        </div>

    )
}